node_modules
dist
.DS_Store
*.log
.env
.vite

# Local screenshots / scratch
Capture*.PNG
Capture*.png

# Source / reference assets that get processed into public/ — keep the
# originals on disk for editing but don't ship them.
Daybreak/
*.m4a
medieval council table.png
